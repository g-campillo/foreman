node_modules/
out/

# electron-builder output: ~731M, and it contains an unpacked copy of the SDK's
# 256MB `claude` binary. Never let this near a commit.
release/

*.tsbuildinfo
.DS_Store

# Notarization credentials (APPLE_ID, APPLE_APP_SPECIFIC_PASSWORD, APPLE_TEAM_ID)
# belong in the environment, never in the repo.
.env
.env.*

# Serena's per-project language-server cache. Written by whichever editor or
# agent has the MCP server attached, so it is local tooling state, not ours.
.serena/
